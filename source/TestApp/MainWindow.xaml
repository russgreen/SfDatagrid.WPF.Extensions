<Window x:Class="TestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:local="clr-namespace:TestApp"
        xmlns:viewmodel="clr-namespace:TestApp.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <viewmodel:ViewModel/>
    </Window.DataContext>

    <Grid>
        <syncfusion:SfDataGrid x:Name="ordersGrid"   
                               AllowEditing="True" 
                               SelectionMode="Extended"
                               AutoGenerateColumns="False"                                     
                               ItemsSource="{Binding OrderInfoCollection }"
                               SelectedItems="{Binding SelectedOrderInfoCollection, Mode=TwoWay}"
                               CurrentCellValidated="sfDataGridSheets_CurrentCellValidated">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridCheckBoxSelectorColumn MappingName="SelectorColumn" HeaderText="" AllowCheckBoxOnHeader="True" Width="34" />
                <syncfusion:GridTextColumn MappingName="CustomerId" HeaderText="CustomerId" AllowEditing="False"/>
                <syncfusion:GridTextColumn MappingName="CustomerName" HeaderText="CustomerName"/>
                <syncfusion:GridTextColumn MappingName="Country" HeaderText="Country"/>
                <syncfusion:GridTextColumn MappingName="ShippingCity" HeaderText="ShippingCity"  />
                <syncfusion:GridTextColumn MappingName="Quantity" HeaderText="Quantity"  />
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>
    </Grid>
</Window>
